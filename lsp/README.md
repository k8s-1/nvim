Config in this directory is automatically managed by the nvim-lspconfig plugin.

Otherwise, we would need to configure language servers manually here e.g.
./lsp/<language_server>.lua

To override default behaviour, configure ./after/lsp/<language_server>.lua
